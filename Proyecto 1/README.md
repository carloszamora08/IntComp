# Hogwarts Sorting Hat ML App

## Setup Instructions

1. Create a virtual environment:
   python -m venv venv

2. Activate it:
   Windows:
   venv\Scripts\activate

3. Install dependencies:
   pip install -r requirements.txt

4. Run the app:
   streamlit run app.py

The app will open at:
http://localhost:8501